<section ng-controller="ThingsController" ng-init="findOne()">
  <div class="page-header">
    <h1 ng-bind="thing.title"></h1>
  </div>
  <div class="pull-right" ng-show="authentication.user._id == thing.user._id">
    <a class="btn btn-primary" ui-sref="things.edit({thingId: thing._id})">
      <i class="glyphicon glyphicon-edit"></i>
    </a>
    <a class="btn btn-primary" ng-click="remove();">
      <i class="glyphicon glyphicon-trash"></i>
    </a>
  </div>
  <small>
    <em class="text-muted">
      Posted on
      <span ng-bind="thing.created | date:'mediumDate'"></span>
      by
      <span ng-bind="thing.user.displayName"></span>
    </em>
  </small>
  <p class="lead" ng-bind="thing.content"></p>
</section>
